# Simple NGINX with PHP-FPM Setup using `docker compose`

## Exposing
NGINX will listen on Port `8080`. Can be changed in the `docker-compose.yml` file.

## Start
```bash
docker compose up
```

